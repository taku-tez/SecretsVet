# Helm values file with secrets — used by SV6010 tests
# This file is intentionally named with a generic name; tests reference it by constructing
# a Resource with File set to "values.yaml".

database:
  host: postgres.svc.cluster.local
  port: 5432
  password: "supersecretpassword123"   # SV6010 HIGH — suspicious key + value

aws:
  accessKeyId: AKIAIOSFODNN7EXAMPLE    # SV6010 HIGH — AWS key pattern
  secretAccessKey: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

payment:
  apiKey: xK9mP2nQrV4wZ8aB3cE6fH1jL5oT7yU0  # SV6010 MEDIUM — high-entropy token

github:
  token: ""    # empty — should NOT trigger

app:
  replicas: 3          # numeric — should NOT trigger
  image: nginx:latest  # not suspicious key

placeholders:
  apiKey: "changeme"   # placeholder — should NOT trigger
  password: "replace-me"  # placeholder — should NOT trigger
